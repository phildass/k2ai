# Environment Configuration
ENVIRONMENT=development

# API Keys
OPENAI_API_KEY=your_openai_api_key_here
# Alternative: Use Azure OpenAI
# AZURE_OPENAI_API_KEY=your_azure_openai_key
# AZURE_OPENAI_ENDPOINT=your_azure_endpoint

# CORS Origins (comma-separated)
# Include all development server origins (Next.js on 3000, simple HTTP server on 8080/8081)
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:8081,http://127.0.0.1:3000,http://127.0.0.1:8080,http://127.0.0.1:8081

# LLM Configuration
LLM_MODEL=gpt-4-turbo-preview
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=1000

# Translation API (optional)
# AZURE_TRANSLATOR_KEY=your_azure_translator_key
# AZURE_TRANSLATOR_REGION=your_region

# Database (if needed for conversation history)
# DATABASE_URL=postgresql://user:password@localhost:5432/k2ai

# Email Configuration (for lead capture)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your_email@example.com
# SMTP_PASSWORD=your_password

# Calendar Integration (optional)
# GOOGLE_CALENDAR_CREDENTIALS=path/to/credentials.json

# WhatsApp/Slack Integration (optional)
# WHATSAPP_API_KEY=your_whatsapp_key
# SLACK_BOT_TOKEN=your_slack_token
